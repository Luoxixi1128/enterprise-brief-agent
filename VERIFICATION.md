# 免费公开部署版验证记录

日期：2026-09-20。状态：本地验证通过，尚未上线。本记录不等同于 Render / Supabase 线上验收，也不代表真实模型质量通过率。

## 已执行

执行 `python -m unittest -v test_public test_cloud`，**29项测试全部通过**（11项原公开接口回归 + 18项云存储集成）。

云存储测试使用 PostgreSQL/WASM 引擎 PGlite 0.5.8 执行正式 `supabase/schema.sql`，本机回环 HTTP 服务模拟 Supabase REST 网关与私有对象存储；模型返回模拟结果。没有调用真实公司接口，也没有连接真实云项目。

### 流程与保存

- 模拟模型完成 Agent 生成、自然语言批量确认与输出格式选择；手动编辑、填写确认答案仍可用。
- 真实生成并读取 Word / Excel 文件，历史任务书可下载；预览临时文件使用后清理。
- 清空整个本地目录并更换缓存路径后，使用原 Cookie 和独立存储恢复任务、确认答案、历史版本、原文件、Word / Excel 和调用计数。下载内容与重启前逐字节一致。
- 重启前正在处理的任务恢复为暂停，保留原运行ID，可继续原检查点；缓存路径变化不会误判需求已变更。
- 云端写入或文件上传失败时返回503，不返回保存成功，也不使用本地任务数据库作为后备。
- 后台线程因存储断连无法保存暂停状态时，连接恢复后可从最后远端检查点继续。
- 达到80个历史版本时重试同一批提交不重复计数；新增版本仍受限制。

### 隔离、权限与限制

- 不同工作区不能互相列出、读取、修改或下载任务及文件，重启后仍隔离。
- Cookie、CSRF、来源和域名检查有效；访客不能修改模型配置，响应不暴露密钥、服务器路径或内部云存储标识。
- 正式 SQL 重复初始化通过；anon / authenticated 角色没有任务表读写与函数执行权限，service_role 拥有函数执行权限，bucket 被设为 private。实际平台网关及对象存储权限仍待上线验证。
- 模型请求计数在并发竞争下原子生效，失败请求计数，预算耗尽时不调用模型；重启不重置计数。
- 任务、上传、数据库正文、文件容量和访客数量限制的相关路径通过；存储已满时仍可读已有数据和暂停任务。
- 第二个服务实例不能同时取得写入权。租约转移后，旧实例不能写入任务或继续预留模型调用额度。
- 云文件下载核对 SHA-256；损坏的远端文件不会作为正常文件交付。

前端 JavaScript 语法检查及改动的 Python 模块编译检查通过。

## 之前已完成的生产入口检查

本免费存储改造之前，公开接口版已在隔离目录用 Gunicorn 23.0.0 单 worker 启动；浏览器验证过首页、新建任务、默认提交需求页、四步导航及无模型时保存补充说明。本轮没有把该结果算作免费云端部署验证。

## 发布检查

发布包使用明确的源码白名单，排除真实任务、上传和导出文件、`.env`、本地虚拟环境、`node_modules`、QA产物与历史备份。封装时重新核查原 V5.0 源码哈希，扫描本地 API 密钥和私有接口地址是否进入待发布文件。文件清单及哈希随部署准备包保存在项目输出目录。

## 尚未执行

- Render 实际构建、HTTPS公网访问、真实休眠/重启恢复。
- Supabase 真实项目的密钥、REST、SQL权限、私有 bucket 上传下载与平台额度验证。
- 云端连接公司 API、真实模型完整业务流程。
- Docker镜像构建（当前环境没有Docker）。
- 512MB免费实例的多人长文档内存峰值、大规模压力测试、独立安全审计。

下一步须按 DEPLOY.md 完成真实云端验收后，才填写线上地址并宣布公开可用。

# 免费公开部署交接

准备日期：2026-09-20。产品保持 V5.0；本机正式工作台和 FDE X 官网不作改动。

## 当前状态

已准备独立部署源码、Render Free 配置、Supabase 数据库脚本与私有文件适配、访客隔离、限额和本地恢复测试。GitHub、Render、Supabase 已登录，Supabase Free 项目已创建。

Supabase 数据库初始化尚未执行；公开 GitHub 仓库、Render 服务和在线网址尚未创建。未将公司 API 密钥传到平台。GitHub 连接器未登录，浏览器控制连接超时，正在通过用户当前已登录页面继续。不要把本地测试完成理解成已经上线。

## 选定方案与免费边界

| 平台 | 用途 | 所选档位 |
| --- | --- | --- |
| Render | Python 网站、Agent 执行、文件生成 | Free Web Service，单实例，新加坡区域 |
| Supabase | PostgreSQL 任务数据及私有文件存储 | Free 项目，优先相同或临近区域 |
| GitHub | 公开源码与部署来源 | 用户账户下的公开仓库 |

2026-09-20核查：Render 免费服务闲置15分钟会休眠，重新访问时唤醒通常约1分钟；每工作区每月750实例小时，本地文件在重启/休眠后不能依赖。不能挂载持久磁盘；本方案将持久数据移出 Render。[官方说明](https://render.com/docs/free)

Supabase Free 含500MB数据库、1GB文件存储及5GB出站流量，闲置一周可能暂停，最多2个活跃免费项目，不包含自动备份。[官方额度](https://supabase.com/pricing)

这些免费额度有边界，超额可能暂停服务或要求调整方案。不要开启付费档位、升级、自动扩容、付费磁盘或域名购买。若平台要求绑定付费信息或页面显示收费，先停止创建并核实。公司模型 API 的额度单独计算。本代码不通过外部定时访问规避平台休眠。

## 账号就绪后的步骤

1. **创建 GitHub 公开仓库。** 仅上传本目录发布清单内文件，不能上传上一级 FDE X 文件夹。排除 `.env`、私有数据、缓存、测试产物、虚拟环境和 `node_modules`。
2. **创建 Supabase Free 项目。** 数据库密码由账号持有人设置并保管，不要发到聊天里。等待项目初始化完成。
3. 在新项目 SQL Editor 中运行 `supabase/schema.sql`。脚本创建任务表、版本表、配额表和 `brief_cloud` 函数，撤销浏览器角色的表/函数访问权限，并创建私有 bucket `brief-private`。不要将 bucket 改为公开，也不要添加公开读写策略。脚本可重复执行，不导入本机旧任务。
4. 在 Supabase 项目设置中取得项目 URL 和后台 `sb_secret_` 密钥，保存在 Render 的私密环境变量。不要使用 `sb_publishable_`，不要写到浏览器、GitHub、README 或聊天中。[密钥官方说明](https://supabase.com/docs/guides/getting-started/api-keys)
5. 在 Render 用仓库的 `render.yaml` 创建 Blueprint。核对 Web Service 为 **Free**，没有磁盘、收费数据库或其他付费资源，然后填写下面的秘密配置。
6. 等待构建，记录 Render 分配的 HTTPS 地址。Render 自动设置 `RENDER_EXTERNAL_HOSTNAME`；无需另购域名。若使用自定义域名，再配置准确的 `PUBLIC_ORIGIN`。
7. 完成线上验收后，才对外分享网址并更新本文件的上线记录。

### 环境变量

| 名称 | 配置 |
| --- | --- |
| `PUBLIC_STORAGE_BACKEND` | `supabase`，配置文件已设置 |
| `BRIEF_DATA_DIR` | `/tmp/brief-agent`，仅缓存，可丢弃 |
| `SUPABASE_URL` | 新项目的 HTTPS origin，无结尾路径 |
| `SUPABASE_SECRET_KEY` | 后台 `sb_secret_` 密钥，仅平台秘密配置 |
| `PUBLIC_SESSION_SECRET` | Blueprint 生成，部署更新时必须保持不变 |
| `BRIEF_API_BASE_URL` | 公司模型的兼容接口地址 |
| `BRIEF_MODEL` | 公司接口支持的模型标识 |
| `BRIEF_API_KEY` | 公司 API 密钥，仅平台秘密配置 |
| `PUBLIC_ORIGIN` | Render 默认域名可省略；自定义域名时填 HTTPS origin |

会话密钥不是模型密钥；重新生成会使所有访客 Cookie 失效。不要因普通重启或代码升级而更换。配置就绪后，首页能读写数据才说明云连接成功，`/healthz` 只证明应用进程在响应。

## 线上验收

- HTTPS 首页和 V5.0 四步流程可用。
- 两个独立浏览器建立任务，不能互相列出、读取、修改或下载数据。
- 自拟材料跑真实模型：生成 → 补充修改 → 全部确认并选择业务输出格式 → 人工审阅 → 下载实际 Word / Excel。
- 手动逐节修改和填写确认答案仍可用，密钥、模型接口地址、服务器文件路径不出现在浏览器响应里。
- Render 重启或休眠唤醒后，原 Cookie 的任务、确认答案、历史版本、原文件和导出文件仍可取回。
- 中断任务显示可继续；存储异常时明确报错，不把本地暂存当成成功保存。
- 匿名/publishable key 无法访问任务表、函数和私有 bucket；用真实 Supabase API 验证此项，本地替身不覆盖平台网关配置。
- 检查平台内存、数据库实际空间、文件容量、日志与模型用量。

## 维护、备份和回退

- 使用一个 Gunicorn worker 和一个实例，不设置 preload、自动按请求数回收或自动扩容。自动部署已关闭，更新前等任务停止。
- 数据库保存75秒单实例租约，运行期间续约。新旧部署交替时，新实例可能需等待旧租约过期；旧实例失去租约后不能继续保存或发起下一次模型请求。这不是多实例任务队列。
- 中断恢复以已保存检查点为准，模型已返回但尚未保存的步骤可能重做；不能保证没有重复计费。
- Supabase 连接故障不会降级写入 Render 本地。处理线程保存失败后，恢复连接可将任务标为暂停并继续。
- 备份应同时包含 PostgreSQL 的 `brief_*` 业务表、私有 bucket 对象和单独保管的会话密钥。数据库备份不等于文件备份；不要只备份 Render 临时目录。先暂停写入再备份，备份放私有位置，恢复后验证 Cookie 和下载。Free 不包含自动备份，本准备版尚未配置定时备份。
- `brief_files` 的失败上传预留和未关联文件仍占配额。需要清理时先核对任务引用与备份；本版不自动永久删除数据。
- 回退只回退兼容当前 schema 的代码，保留 Supabase 项目、私有对象和原会话密钥。不要将云端实例切回 `local`；这会读不到云任务。本机 V5.0 工作台可独立继续使用，不自动同步公众数据。

## 上线记录

| 项目 | 当前状态 |
| --- | --- |
| GitHub 仓库 | 浏览器已登录，待创建 |
| Render / Supabase 账号 | 已登录；Supabase Free 项目已创建，待初始化数据库 |
| 付费资源 | 未创建，选用 Free 方案 |
| 线上网址 | 未部署 |
| 公司 API | 已获准公众使用，尚未写入云端 |
| 线上真实模型验收 | 未执行 |

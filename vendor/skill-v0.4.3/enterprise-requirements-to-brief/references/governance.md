# 版本与发布责任

- 本包内 `VERSION.json` 记录版本、状态、依赖和回滚来源。`candidate` 只表示已完成包校验，不表示企业已批准发布。
- Skill 维护者负责修改规则、更新测试和生成候选包；企业任务负责人负责确认任务范围、数据授权、KR 评分口径和评分人。
- 发布前至少需要一次非作者复核，并用两类真实输入完成运行与结果抽查。复核身份、版本哈希和结论写入外部评审记录，不在包内伪造签名。
- 回滚时按 `VERSION.json` 指定的原始包哈希核对后重装；不要直接覆盖唯一副本。
